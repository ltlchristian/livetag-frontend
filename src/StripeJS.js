import React from "react";
import CheckoutForm from "../src/stripe/CheckoutForm";
import StripeContainer from "../src/stripe/StripeContainer";

function StripeJS() {
  return (
    <div>
      <h1>StripeJS</h1>
      <StripeContainer />
    </div>
  );
}

export default StripeJS;
